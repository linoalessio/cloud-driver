.venv/
__pycache__/
*.py[cod]
.pytest_cache/
dist/
build/
*.egg-info/
# The local vector store - derived data, never committed (and it holds embeddings of real file
# content, see this module's README on why that matters).
chroma/
